package com.f012r.naverbooking.domain.reservations.repository;

import com.f012r.naverbooking.domain.reservations.entity.ReservationUserCommentImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReservationUserCommentImageRepository extends JpaRepository<ReservationUserCommentImage, Integer> {
}
